import <std/list>;

fn main() -> int {
    let xs: List;
    xs.append("robot");
    xs.append("cv");
    xs.append("control");
    print(list.join_str(xs, " | "));
    let rev: List = list.reverse(xs);
    print(list.join_str(rev, " -> "));
    return 0;
}
