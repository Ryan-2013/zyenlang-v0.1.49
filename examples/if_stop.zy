fn divide(a: float, b: float) -> float {
    if (b == 0) {
        stop "divide by zero";
    }

    return a / b;
}

fn main() -> int {
    let x: float = divide(10.0, 2.0);
    print(x);
    return 0;
}
