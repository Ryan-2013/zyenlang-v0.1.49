import <std/math>;

fn add(a: int, b: int) -> int {
    return a + b + 1000;
}

fn main() -> int {
    let a: int = add(1, 2);
    let b: int = math.clamp_int(120, 0, 100);

    print(a);
    print(b);

    return 0;
}
